![image-1781884616318.png](../../assets/03%20全排列/image-1781884616318.png)

## 思路
每个元素当一次头，之后循环找没有出现过的元素

## 代码1

```cpp
class Solution {
public:
    vector<vector<int>> permute(vector<int>& nums) {
        vector<vector<int>> ans;
        vector<int> path;
        vector<bool> used(nums.size(), false); // 用来标记元素是否使用
        dfs(nums, 0, ans, path, used);
        return ans;
    }
    void dfs(vector<int>& nums, int i, vector<vector<int>>& ans, vector<int>& path, vector<bool>& used)
    {
        if(i == nums.size())
        {
            ans.push_back(path);
            return;
        }
        for(int j = 0; j < nums.size(); j++)
        {
            if(!used[j]) // 判断是否使用过
            {
                path.push_back(nums[j]);
                used[j] = true;
                dfs(nums, i + 1, ans, path, used);
                path.pop_back();
                used[j] = false;
            }
        }
    }
};
```

## 代码2
```cpp
class Solution {
public:
    vector<vector<int>> permute(vector<int>& nums) {
        vector<vector<int>> ans;
        dfs(nums, 0, ans);
        return ans;
    }
    void dfs(vector<int>& nums, int i, vector<vector<int>>& ans)
    {
        if(i == nums.size())
        {
            ans.push_back(nums);
            return;
        }
        for(int j = i; j < nums.size(); j++)// 固定一个位置i，让之后的每个元素都到i一遍, j 要先等于 i 因为自己不变也算一个分支
        {
            swap(nums, i, j); 
            dfs(nums, i + 1, ans);
            swap(nums, i, j);
        }
    }
    void swap(vector<int>& nums, int a, int b)
    {
        int temp = nums[a];
        nums[a] = nums[b];
        nums[b] = temp;
    }
};
```

### 讲解
![image-1782143751803.png](../../assets/03%20全排列/image-1782143751803.png)
i 是一个固定的位置，从后面把每个j都拉过来i的位置一次
从 i 开始，后面的每个·元素都来当一次 i ；递归找下面的